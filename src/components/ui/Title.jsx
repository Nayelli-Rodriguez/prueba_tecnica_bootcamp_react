function Title({ children }) {
    return (
        <h1 className="text-center text-3xl lg:text-4xl font-semibold text-white uppercase">
            {children}
        </h1>
    );
}

export default Title;
